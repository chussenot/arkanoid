
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# Everything pact or an agent writes under .pact/ is local runtime state,
# EXCEPT the append-only event log, which is history and belongs in git.
.pact/*
!.pact/messages.jsonl
!.pact/events.jsonl


# Added by cargo

/target

# LBreakoutHD levelsets fetched by scripts/fetch-levelsets.sh: GPLv3 data,
# never committed to this MIT repo. See ATTRIBUTION for source/license.
/levels/

# Kenney puzzle pack fetched by scripts/fetch-assets.sh: CC0, but left
# gitignored like this repo's other fetched asset script rather than
# committed as binary blobs. See ATTRIBUTION for source/license.
/assets/pack/
